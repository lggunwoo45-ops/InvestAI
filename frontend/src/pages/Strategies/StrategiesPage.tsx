import { PagePlaceholder } from '@/components/PagePlaceholder/PagePlaceholder'

export function StrategiesPage() {
  return <PagePlaceholder title="Strategies" description="Versioned strategy definitions, validation, and deployment." icon="strategies" capabilities={['Rule composition', 'Backtest boundaries', 'Versioned deployments']} />
}
